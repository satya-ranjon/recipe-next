import { Inter, Lobster } from "next/font/google";

// define your variable fonts
const inter = Inter({ subsets: ["latin"] });
const lobster = Lobster({ subsets: ["cyrillic"], weight: "400" });

export { inter, lobster };
